
a<-1
a

b<-2
b

c<-3
c

d<-3.5
d
#여기까지 스크립트를 만들면 우측 변수값들이 실시간으로 보인다. 확인해보자.

#연산을 해보자

a+b
a+b+c
4/b
5*b
d*c

#다음은 c()함수를 이용하는 예제이다. 참고로 C는 Combine의 약자이다
#또한 함수는 어떠한 기능을 하는 명령이다

var1 <- c(1,2,3,4,5)
var1

var2 <-c(1:5) #콜론(:)을 이요하면 연속적인 값을 쉽게 생ㅅ
var2

var3<-seq(1,5) #seq()함수로도 연속값을 지닌 변수 만들수 있음

var4 <- seq(1, 10, by = 2) #2씩 증ㄱ

var5 <- seq(1,10,by=3)가

